import XCTest
@testable import CloudResourcesFoundation

final class CloudResourcesFoundationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CloudResourcesFoundation().text, "Hello, World!")
    }
}
